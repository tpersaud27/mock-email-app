import { EmailViewPlaceholderComponent } from './email-view-placeholder.component'

describe('EmailViewPlaceholderComponent', () => {
  it('should mount', () => {
    cy.mount(EmailViewPlaceholderComponent)
  })
})